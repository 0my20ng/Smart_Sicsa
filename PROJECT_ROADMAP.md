# 🚀 Smart Sicsa 프로젝트 로드맵 (업데이트: 2026-05-09)

대학생 맞춤형 식사 가이드 서비스의 개발 진행 상황과 향후 계획입니다.

---

## ✅ 1. 완료된 사항 (Completed)

### 🎨 프론트엔드 디자인 & 레이아웃
- [x] **로그인 페이지**: 세련된 그린 테마 UI 및 소셜 로그인 버튼 구현.
- [x] **사이드바 시스템**: 
    - [x] 모든 기기(데스크톱/모바일)에서 작동하는 토글형 드로어 구현.
    - [x] 인라인 스타일 기반 물리적 트랜지션 및 최상단 레이어(z-index) 보장.
    - [x] 사용자 핸드드로잉 스케치를 반영한 디자인 고도화 (형광 초록 포인트).
    - [x] 클릭 시 페이지 이동 내비게이션 기능 연결.
- [x] **레시피 추천 화면 (`/main`)**: 스케치 기반 [재료 태그 + 검색창 + AI 레시피 리스트] 레이아웃 구현.
    - [x] 재료 태그 추가 (Enter 키 입력)
    - [x] 재료 태그 X 버튼으로 개별 삭제
    - [x] 레시피 클릭 시 상세 뷰 전환 (보유/부족 재료 구분, 원본 레시피 이동 버튼)
- [x] **식당 추천 화면 (`/restaurant`)**: 스케치 기반 [좌측 필터 패널 + 우측 지도 영역 + 위치 버튼] 레이아웃 구현.

### 🤖 AI 레시피 추천 및 백엔드 (Python FastAPI, `Backend/create_food.py`)
- [x] **재료 관리 API**: `GET /ingredients` (조회) 및 `POST /ingredients` (추가) 구현 완료.
- [x] **레시피 추천 API (`POST /recommend-recipes`)**: 보유 재료 및 검색어(`query`)를 받아 Gemini AI에 요청.
    - 사용 SDK: 신규 `google-genai` 패키지 적용.
    - 사용 모델: `gemini-3.1-flash-lite` + 통합된 `google_search` 그라운딩 도구 활용.
    - 블로그 URL, 대표 이미지, 보유/부족 재료, 요리 설명을 JSON 형태로 반환.
    - API 할당량 초과(429) 및 빈 재료 에러(400) 예외 처리 완료.
    - 환경 변수: `Backend/.env` 분리 및 `python-dotenv` 적용.
- [x] **CORS 설정**: Next.js 프론트엔드(`localhost:3000`)와의 교차 출처 통신 허용.

### ⚙️ 시스템 및 환경 설정
- [x] **네트워크 설정**: 외부 기기(모바일 등)에서 HMR 접속 가능하도록 `allowedDevOrigins` 설정 완료.
- [x] **Python 가상환경**: `venv`를 이용한 독립 패키지 관리 구조 도입 (`.gitignore`에 `venv/` 추가).
- [x] **API 가이드 문서**: `API_GUIDE.md` 작성 - 필요한 환경 변수 및 API 키 발급 방법 안내.

---

## 🔧 2. 현재 진행 중 / 개선 필요 사항

### 🍱 레시피 추천 페이지 (`/main`)
- [ ] **백엔드 재료 영구 저장**: Python 백엔드의 `ingredients_db`가 서버 메모리에만 존재하여 서버 재시작 시 초기화됨. SQLite 또는 파일 저장 방식으로 영구 저장 필요.
- [ ] **재료 삭제 API**: 현재 재료 삭제는 프론트엔드 로컬 상태에서만 처리됨. Python 백엔드에 `DELETE /ingredients/{name}` API 추가 필요.
- [ ] **이미지 로딩 오류 처리**: Gemini가 반환한 `imageUrl`이 깨지는 경우 대체 이미지 표시 로직 추가.
- [ ] **입력 자동완성**: 재료 타이핑 시 추천 검색어 노출.

### 📍 식당 추천 페이지 (`/restaurant`)
- [ ] **지도 API 연동**: 네이버 지도 또는 카카오 지도 실제 SDK 연동 (현재 플레이스홀더).
- [ ] **커스텀 마커 구현**: 식당 카테고리별 아이콘 표시.
- [ ] **상세 정보 모달**: 식당 클릭 시 나타나는 상세 팝업창 구현.

---

## 🖥️ 3. 백엔드 (Backend) 구축 필요 기능

### 🔐 인증 및 사용자 데이터
- [ ] **User API**: 실제 회원가입 및 로그인 데이터베이스 연동.
- [ ] **Refrigerator API**: 사용자별 냉장고 재료 영구 저장 및 CRUD 기능 (DB 연동).
- [ ] **Bookmark API**: 찜한 레시피 및 식당 목록 저장.

### 📖 데이터 서비스
- [ ] **Recipe matching API**: 보유 재료 기반 최적 레시피 검색 엔진 고도화.
- [ ] **Restaurant data API**: 위치 기반 식당 정보 제공 및 필터링 검색.

---

## 🗄️ 4. 데이터베이스 (Database) 설계

- [ ] **Users**: 계정 및 선호도
- [ ] **Recipes**: 요리 정보 및 재료 매핑
- [ ] **Restaurants**: 위치 및 메뉴 정보
- [ ] **Bookmarks**: 즐겨찾기 연동 테이블

---

## 🌟 5. 향후 추가 계획 (V2)

- [ ] **전역 상태 관리**: Zustand 등을 이용하여 선택한 재료를 페이지 이동 시에도 유지.
- [ ] **커뮤니티**: 대학생 자취 요리 팁 공유 게시판.
- [ ] **AI 재료 분석**: 냉장고 재료 사진 업로드 시 자동 인식 기능.
