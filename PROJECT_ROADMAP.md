# 🚀 Smart Sicsa 프로젝트 로드맵 (업데이트: 2026-06-06)

대학생 맞춤형 식사 가이드 서비스의 개발 진행 상황과 향후 계획입니다.

---

## ✅ 1. 완료된 사항 (Completed)

### 🎨 프론트엔드 디자인 & 레이아웃
- [x] **로그인 페이지**: 세련된 그린 테마 UI 및 소셜 로그인 버튼 구현.
- [x] **사이드바 시스템**:
    - [x] 모든 기기(데스크톱/모바일)에서 작동하는 토글형 드로어 구현.
    - [x] 인라인 스타일 기반 물리적 트랜지션 및 최상단 레이어(z-index) 보장.
    - [x] 사용자 핸드드로잉 스케치를 반영한 디자인 고도화 (형광 초록 포인트).
    - [x] 클릭 시 페이지 이동 내비게이션 기능 연결.
    - [x] 미구현 기능(냉장고, 북마크)에 "로그인 필요" 잠금 상태 표시.
- [x] **레시피 추천 화면 (`/main`)**: 스케치 기반 [재료 태그 + 검색창 + AI 레시피 리스트] 레이아웃 구현.
    - [x] 재료 태그 추가 (Enter 키 입력)
    - [x] 재료 태그 X 버튼으로 개별 삭제
    - [x] 레시피 클릭 시 상세 뷰 전환 (보유/부족 재료 구분, 원본 레시피 이동 버튼)
- [x] **오늘의 레시피 화면 (`/daily`)**: 날짜 기반 시드로 일일 음식 추천 + 추천 이유 표시.
    - [x] 돌림판 스핀 애니메이션으로 결과 공개
    - [x] 15가지 음식 풀 + 각 추천 이유 및 태그 제공
    - [x] 하루 동안 동일한 추천 유지 (자정에 변경)
- [x] **식당 추천 화면 (`/restaurant`)**: 스케치 기반 [좌측 필터 패널 + 우측 지도 영역 + 위치 버튼] 레이아웃 구현.

### 🗺️ 지도 API 연동 (`/restaurant`)
- [x] **카카오 Maps JS API 연동**: 지도 생성, 줌/타입 컨트롤, 마커, InfoWindow 구현.
- [x] **현재 위치 이동**: Geolocation API → 카카오 CustomOverlay 현재 위치 마커.
- [x] **카카오 로컬 REST API 연동**: 키워드 기반 주변 음식점 검색 (`category_group_code=FD6`).
- [x] **서버사이드 프록시**: REST API 키 보호를 위해 `/api/restaurants` Next.js API 라우트 구현.
- [x] **필터링**: 음식 종류(한식/중식/일식/양식/분식 등), 검색 반경(500m ~ 5km) 선택.
- [x] **가격대 필터 (키워드 우회)**: "저렴한" 등 키워드를 조합하여 가격 힌트 검색 제공.
- [x] **음식점 클릭 InfoWindow**: 이름, 카테고리, 주소, 전화번호, 카카오맵 링크 표시.
- [x] **좌측 결과 리스트**: 검색된 음식점 목록 클릭 시 지도 해당 위치로 이동.

### 🤖 AI 레시피 추천 및 백엔드 (Python FastAPI)
- [x] **재료 관리 API**: `GET /ingredients` 및 `POST /ingredients` 구현 완료.
- [x] **레시피 추천 API (`POST /recommend-recipes`)**: Gemini AI + `google_search` 그라운딩.
- [x] **CORS 설정**: Next.js 프론트엔드와의 교차 출처 통신 허용.

### ⚙️ 시스템 및 환경 설정
- [x] **네트워크 설정**: 외부 기기(모바일 등)에서 HMR 접속 가능하도록 `allowedDevOrigins` 설정.
- [x] **Python 가상환경**: `venv`를 이용한 독립 패키지 관리 구조 도입.
- [x] **API 가이드 문서**: `API_GUIDE.md` 작성 (Google + 카카오 API 키 발급 방법 안내).

---

## 🔧 2. 현재 진행 중 / 개선 필요 사항

### 🍱 레시피 추천 페이지 (`/main`)
- [ ] **백엔드 재료 영구 저장**: `ingredients_db`가 서버 메모리에만 존재하여 서버 재시작 시 초기화됨. SQLite 또는 파일 저장 방식으로 영구 저장 필요.
- [ ] **재료 삭제 API**: 현재 프론트엔드 로컬 상태에서만 처리됨. `DELETE /ingredients/{name}` API 추가 필요.
- [ ] **이미지 로딩 오류 처리**: Gemini가 반환한 `imageUrl`이 깨지는 경우 대체 이미지 표시 로직 추가.
- [ ] **입력 자동완성**: 재료 타이핑 시 추천 검색어 노출.

### 🌟 오늘의 레시피 페이지 (`/daily`)
- [ ] **AI 기반 추천으로 고도화**: 현재는 정적 데이터 풀에서 날짜 기반 선택. Gemini API를 통해 날씨/계절/요일 등을 반영한 동적 추천으로 발전 가능.
- [ ] **레시피 연결**: 추천된 음식을 클릭하면 `/main` 레시피 추천으로 연결.

### 📍 식당 추천 페이지 (`/restaurant`)
- [ ] **커스텀 마커 구현**: 음식 카테고리별 아이콘으로 마커 차별화.
- [ ] **페이지네이션**: 검색 결과가 15건 이상일 경우 다음 페이지 로드.

---

## 🔐 3. 로그인 구현 이후 활성화할 기능 (현재 보류)

> 사이드바에서 "로그인 필요" 상태로 잠금 처리됨.

- [ ] **내 냉장고**: 사용자별 보유 재료 영구 저장 및 CRUD.
- [ ] **레시피 북마크**: 찜한 레시피 저장 및 관리.
- [ ] **식당 북마크**: 저장한 맛집 목록.

### 🔐 인증 구현 계획
- [ ] **User API**: 회원가입 및 로그인 데이터베이스 연동.
- [ ] **Refrigerator API**: 사용자별 냉장고 재료 영구 저장 및 CRUD (DB 연동).
- [ ] **Bookmark API**: 찜한 레시피 및 식당 목록 저장.

---

## 🗄️ 4. 데이터베이스 (Database) 설계

- [ ] **Users**: 계정 및 선호도
- [ ] **Recipes**: 요리 정보 및 재료 매핑
- [ ] **Restaurants**: 위치 및 메뉴 정보
- [ ] **Bookmarks**: 즐겨찾기 연동 테이블

---

## 🌟 5. 향후 추가 계획 (V2)

- [ ] **전역 상태 관리**: Zustand 등을 이용하여 선택한 재료를 페이지 이동 시에도 유지.
- [ ] **커뮤니티**: 대학생 자취 요리 팁 공유 게시판.
- [ ] **AI 재료 분석**: 냉장고 재료 사진 업로드 시 자동 인식 기능.
